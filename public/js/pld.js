$(document).ready(function(){
	$('#galleryForm').transloadit({
	  wait: true,
	  autoSubmit: true,
	  fields: true
	})

	$('#MyForm').transloadit({
	  wait: true,
	  autoSubmit: true,
	  fields: true
	})
})